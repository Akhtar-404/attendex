import 'package:flutter/material.dart';

class EmpHistoryPage extends StatelessWidget {
  const EmpHistoryPage({super.key});
  @override
  Widget build(BuildContext context) {
    return const SafeArea(child: Center(child: Text('History (coming soon)')));
  }
}
